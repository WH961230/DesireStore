---
description: LazyPanPro 预制体生成模板（按输入字段生成/修改 Prefab，联动行为脚本）
alwaysApply: false
---

# LazyPanPro 预制体生成规则模板

## 目的

当用户提供“预制体名（UI/物体）/ 操作描述 / 组件建议 / 行为脚本参考”等字段时，AI 需要：

- 生成或修改一个 Unity 预制体（Prefab）的**结构设计方案**（必要时给出可直接照做的步骤/清单）
- 基于“行为脚本参考”自动推导需要的**子物体层级、组件挂载、引用绑定**
- 给出 LazyPanPro 项目内的**落盘路径建议**（如 `Assets/LazyPan/Bundles/Prefabs/UI` / `Obj` / `Tool` / `Global`）

> 说明：由于 AI 无法直接在 Unity 编辑器里点击创建对象，本规则要求输出**可操作的“Prefab 结构蓝图 + 绑定清单”**。若项目已有自动生成/配置驱动（例如 CSV/Config/Addressables/Loader 约定），输出要同步给出对应的配置挂接点。

---

## 用户必须提供的输入（严格）

每次生成预制体时，用户至少提供以下 4 项：

1. **预制体名（UI/物体）**
   - 示例：`UI_MainMenu`、`UI_Loading`、`Obj_Player`、`Obj_Door`、`Tool_DamageText`
2. **操作描述**
   - 描述这个预制体的用途、交互、生命周期、需要支持的状态（显示/隐藏/可交互/禁用等）
3. **组件建议**
   - 预制体根节点与关键子节点需要的 Unity 组件（如 `RectTransform`、`CanvasGroup`、`Button`、`TMP_Text`、`Image`、`Animator`、`Rigidbody`、`Collider`、`NavMeshAgent` 等）
4. **行为脚本参考**
   - 行为脚本类名/文件路径/关键 API，或“行为名 + 功能描述”
   - 重点：哪些字段需要从 prefab 绑定引用（例如 `Button`、`Transform`、`Animator`、`AudioSource`、`Comp`、子物体路径）

推荐额外字段（可选，但强烈建议）：

- **资源约束**：是否走 Addressables、资源地址规则、是否需要异步加载
- **UI 风格**：分辨率适配、SafeArea、字体、图集、动效（Tween/Animator）
- **交互与可访问性**：按钮热区、键盘/手柄导航、默认选中、音效
- **物体物理约束**：碰撞层、刚体约束、触发器/碰撞器尺寸、Tag/Layer
- **引用绑定方式**：是否使用 LazyPan 的 `Comp/Cond` 之类路径绑定组件

---

## 输出必须包含的内容（严格）

生成结果至少包含以下部分（按顺序）：

1. **预制体摘要**
   - 1-3 句说明用途、类型（UI/物体）、典型使用场景
2. **推荐落盘路径与加载地址**
   - 路径（例如 `Assets/LazyPan/Bundles/Prefabs/UI/<Prefab>.prefab`）
   - 若使用 `Loader`/Addressables：给出推荐 address/key（尽量贴近项目现有约定）
3. **Prefab 层级蓝图（Hierarchy Blueprint）**
   - 用树形结构列出：根节点、子节点、命名、用途
4. **组件挂载清单（Component Checklist）**
   - 每个关键节点：需要的组件、关键属性（例如 Button 的 onClick 绑定策略、CanvasGroup 的交互开关）
5. **行为联动映射（Behaviour ↔ Prefab Bindings）**
   - 从“行为脚本参考”推导：
     - 行为脚本挂载在哪个节点
     - 行为需要的引用字段 → prefab 中对应组件/子物体
     - 若引用缺失：需要新增哪些子物体/组件
6. **LazyPan `Comp` / `Data` 绑定清单（必须）**
   - 若行为/工具代码通过 `entity.Comp.Get<T>(sign)` 或 `Cond.Instance.Get<T>(entity, sign)` 获取引用，则必须在 Prefab 根节点挂 `LazyPan.Comp`，并把对应引用写入 `Comp` 的列表字段（例如 `Buttons` / `Transforms` / `TMPInputFields` / `Images` 等）
   - 若行为/工具代码通过 `entity.Data` 或 `Cond.Instance.GetData(...)` 读取数据，则必须在 Prefab 根节点挂 `LazyPan.Data`
6. **绑定策略**
   - **优先**：通过明确的子物体命名 + 路径查找（如 `Cond.Instance.Get<T>(comp, "Path")`）或序列化字段直绑
   - 明确规定：哪些用“序列化引用”，哪些用“按路径查找”，避免混用造成维护成本
7. **产物清单**
   - 新增/修改的 Prefab、脚本、配置（CSV/Config/Addressables）与原因
8. **验证清单（最小）**
   - 1-5 条如何在场景中验证引用、交互、动效、行为触发正确

---

## 通用预制体生成规则（AI 必须遵守）

### 命名与层级

- **UI Prefab**
  - 根节点：`UI_<Name>`，必须是 `RectTransform`
  - 建议统一子层级：`Root` / `Header` / `Content` / `Footer` / `PopupRoot` / `Mask`
- **Obj Prefab**
  - 根节点：`Obj_<Name>`，必要时包含 `Model` / `Collider` / `Fx` / `Audio` 子节点
- **禁止**：子物体使用“新建游戏对象(1)”等默认命名

### 组件引用与耦合

- **引用优先级**（从高到低）：
  1. 序列化字段直绑（最稳，最清晰）
  2. 固定子物体命名 + 路径查找（适合生成式/可重建 UI）
  3. 运行时 `Find`/全局搜索（尽量避免）
- **缓存**：运行时路径查找结果必须缓存，避免每帧查找

### UI 生成通用规则

- 默认考虑：
  - 分辨率适配（Anchor/Stretch）、SafeArea（若项目支持）
  - 可交互态（CanvasGroup 控制 alpha/blocksRaycasts/interactable）
  - 文本组件使用 TextMeshPro（`TMP_Text` / `TextMeshProUGUI`）
- 交互建议：
  - Button 点击音效与防连点（如需要）
  - 默认选中与导航（如需要手柄/键盘）

### 物体生成通用规则

- 默认考虑：
  - Layer/Tag、碰撞器/触发器、刚体约束
  - 可视模型与逻辑碰撞分离（`Model` vs `Collider`）
  - 可选的 `Gizmos` / 调试开关（若项目已有习惯）

### 行为驱动的“自动补全”规则（关键）

当用户提供“行为脚本参考”时，AI 必须：

- **反推需求**：从行为脚本的字段/方法/说明推导 prefab 需要哪些：
  - 子物体节点（例如 `Button_Start`、`Text_Title`、`SpawnPoint`、`HitBox`）
  - 组件（例如 `Button`、`TMP_Text`、`Animator`、`AudioSource`、`Collider`）
  - 默认参数（例如动画状态名、事件回调占位、音效引用槽位）
- **补全结构**：若缺失就建议创建对应子物体，并给出命名与用途
- **补全引用**：明确每个引用字段应绑定到哪个组件/路径
- **最小化改动**：若是“修改已有 prefab”，尽量在不破坏原层级的前提下增量添加

### LazyPan `Comp` / `Data` 的特殊规则（关键）

- **重要事实**：LazyPan 的 `Comp.Get<T>(sign)` 并不会按场景层级名字查找组件，它是从 `Comp` 组件里维护的各类“Sign → 引用”的列表中取值。
  - 例如行为里写 `entity.Comp.Get<Button>("SaveButton")`，则 Prefab 根节点上的 `Comp.Buttons` 列表中必须存在 `Sign="SaveButton"` 且 `Button` 引用正确。
  - 若只创建了名为 `SaveButton` 的子物体但没有写入 `Comp.Buttons`，运行时仍会取不到。
- **因此生成预制体时必须输出两份映射**：
  1. **层级结构**：哪些子物体/组件需要存在
  2. **Comp 映射表**：每个 `sign` 要写入 `Comp` 的哪个列表，引用指向哪个组件/Transform
- **Data 的要求**：
  - 只要行为代码会访问 `entity.Data`（读写任意数据），Prefab 根节点必须存在 `Data` 组件（即使列表初始为空也可以）。

---

## 交互模板（你每次只要按这个填）

把下面模板贴给 AI 并填写：

```
预制体名（UI/物体）：

操作描述：

组件建议：

行为脚本参考：

（可选）资源约束（Addressables/Loader 约定）：
（可选）UI 风格/适配要求：
（可选）物理/Layer/Tag 约束：
（可选）引用绑定方式偏好（序列化 / 路径查找）：
（可选）是否“新建”还是“修改已有”：
（可选）现有层级截图/描述（若是修改）：
```

---

## AI 生成输出模板（AI 必须按此结构输出）

### 预制体摘要
- …

### 推荐落盘路径与加载地址
- **Prefab 路径**：…
- **加载地址 / Address**：…

### Prefab 层级蓝图（Hierarchy Blueprint）
```
<PrefabRoot>
  Root
    ...
```

### 组件挂载清单（Component Checklist）
- **<NodePath>**
  - 组件：…
  - 关键属性：…

### 行为联动映射（Behaviour ↔ Prefab Bindings）
- **行为挂载点**：…
- **引用绑定表**：
  - `<BehaviourField>` → `<NodePath>/<Component>`
- **需要新增的子物体/组件**：…

### 绑定策略
- …

### 产物清单
- **新增**：…
- **修改**：…

### 验证清单（最小）
- …

