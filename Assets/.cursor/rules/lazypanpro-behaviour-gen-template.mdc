---
description: LazyPanPro 行为生成模板（按输入字段生成 Behaviour）
alwaysApply: false
---

# LazyPanPro 行为生成规则模板

## 目的

当用户提供“行为名 / 操作描述 / 组件建议”等字段时，AI 需要按本规则生成一个可落地的 LazyPanPro 行为脚本（以及必要的配套代码/配置建议），并保持一致的结构、命名与可维护性。

> 说明：本规则是“生成模板与通用约束”。当缺少框架细节（例如 Behaviour 基类、注册机制、配置格式）时，先生成“最小可用版本”，并在输出中列出**需要对齐的框架接口点**，随后再根据项目实际脚本做一次对齐改造。

---

## 用户必须提供的输入（严格）

每次生成行为时，用户至少提供以下 3 项：

1. **行为名**：建议 PascalCase（如 `Behaviour_MoveToTarget` / `Behaviour_OpenDoor`）
2. **操作描述**：一句或几句说明“何时触发、做什么、成功/失败条件、边界情况”
3. **组件建议**：该行为依赖/需要操作的 Unity 组件或数据（如 `Rigidbody`、`NavMeshAgent`、`Animator`、`AudioSource`、`Collider`、`CanvasGroup`、自定义 `Comp` 等）

推荐额外字段（可选，但强烈建议）：

- **触发方式**：Update/FixedUpdate/事件驱动/一次性执行/协程
- **输入参数**：行为需要的参数列表（类型、默认值、范围）
- **输出/结果**：成功/失败/进行中，是否需要回调或事件
- **约束**：性能要求、是否可打断、是否可并发、是否需要网络同步

---

## 输出必须包含的内容（严格）

生成结果至少包含以下部分（按顺序）：

1. **行为摘要**：1-3 句说明行为用途与使用场景
2. **依赖与组件检查**：
   - 必需组件（缺失时如何处理：自动 AddComponent / 报错 / 安全降级）
   - 可选组件（有则用，无则跳过）
3. **参数设计**：
   - 参数列表（名称、类型、默认值、意义）
   - 参数校验（范围/空值/资源存在性）
4. **执行逻辑**：
   - 状态机（开始/进行中/完成/失败/取消）
   - 关键步骤与边界情况
5. **与 LazyPanPro 的接入点**：
   - 行为脚本放置路径建议（优先 `Assets/LazyPan/Scripts/GamePlay/Behaviour/` 或项目约定目录）
   - 若需要注册：说明应在哪个 Register/Config 中挂接（例如可能的 `BehaviourRegister`、CSV 配置或生成器 `Generate`）
6. **产物清单**：
   - 需要新增/修改哪些文件（脚本、配置、预制体、CSV）
7. **测试清单（最小）**：
   - 1-3 条“如何在 Unity 场景里验证它工作”

---

## 通用代码生成规则（AI 必须遵守）

- **命名**：
  - 行为类名与文件名一致，使用 PascalCase。
  - 公有可配置字段使用 PascalCase 或 `[SerializeField] private` + camelCase（二者择一，但同一项目保持一致）。
- **健壮性**：
  - 对组件/引用做空值检查；对参数做边界检查。
  - 行为执行失败时给出明确原因（日志或返回值），不要静默失败。
- **可维护性**：
  - 单个行为只做一件事；复杂流程拆分为多个行为或子步骤。
  - 把可调参数集中在一个参数对象/序列化字段中，避免散落 magic number。
- **性能**：
  - 避免每帧 `Find`/`GetComponent` 重复调用；必要时缓存引用。
  - 避免在 Update 中频繁分配（LINQ、string 拼接、临时 List）。
- **Unity 约束**：
  - 物理相关逻辑放 `FixedUpdate` 或通过 `Rigidbody` API 处理。
  - 动画触发集中处理，避免每帧重复 SetBool/SetTrigger。
- **兼容“生成器”**：
  - 如果项目已有 `Generate.GenerateBehaviour(...)` 风格的代码生成与模板机制，生成物应尽量贴近其模板（类名、命名空间、目录）。

---

## 交互模板（你每次只要按这个填）

把下面模板贴给 AI 并填写：

```
行为名：

操作描述：

组件建议：

（可选）触发方式：
（可选）输入参数：
（可选）输出/结果：
（可选）约束：
（可选）使用示例场景：
```

---

## AI 生成输出模板（AI 必须按此结构输出）

### 行为摘要
- …

### 依赖与组件检查
- **必需组件**：…
- **可选组件**：…
- **缺失处理策略**：…

### 参数设计
- **参数列表**：…
- **校验规则**：…

### 执行逻辑
- **状态**：Ready / Running / Succeeded / Failed / Cancelled（按项目实际对齐）
- **步骤**：…
- **边界情况**：…

### LazyPanPro 接入点
- **建议脚本路径**：…
- **注册/配置挂接**：…
- **需要对齐的框架接口点**：若不确定基类/注册方式，列出待确认项（例如：是否有 `Behaviour` 基类？是否通过 CSV/Config 生成？）

### 产物清单
- **新增文件**：…
- **修改文件**：…
- **资源/预制体**：…

### 代码
在能够确定项目行为基类/命名空间时，直接给出可编译脚本；否则先给出“可编译的 Unity MonoBehaviour 版本 + 对齐点”，待补齐框架细节后再重构为框架行为。

### 测试清单（最小）
- …

