# 商店系统 — Unity 预制体人工设置清单

完成代码实现后，需在 Unity 编辑器中按以下步骤搭建 UI 节点结构。**节点名称必须与下表完全一致**，否则 `Cond.Get` 会找不到组件。

---

## 一、场景与引用

- **场景**：使用 Flow_SceneA 的 UI 根物体（与任务/成就相同）
- **根路径**：`flow.GetUI()` → `界面` → `详情` / `状态栏` / `模板`

---

## 二、Hierarchy 节点树（需新建或确认）

### 2.1 详情下新增「商店」面板

| 父物体 | 子物体名 | 组件 | 说明 |
|--------|----------|------|------|
| `界面/详情` | `商店` | Comp（或 RectTransform + 任意可挂 Comp 的组件） | 商店主面板，与「任务」「成就」同级 |
| `界面/详情/商店` | `父物体` | Transform（或 RectTransform） | 商品列表的挂载父节点，需带 LayoutGroup（如 VerticalLayoutGroup） |

**操作步骤：**

1. 在 Hierarchy 中找到 `界面` → `详情`
2. 在 `详情` 下新建空物体，命名为 **商店**
3. 参考「任务」或「成就」面板的结构：添加 RectTransform、Canvas Group 等，确保可显示/隐藏
4. 在 `商店` 下新建空物体，命名为 **父物体**
5. 给 `父物体` 添加 **Vertical Layout Group**（或 Grid Layout Group），用于自动排列商品项
6. 默认将 `商店` 设为 **SetActive(false)**，与任务/成就一样初始隐藏

---

### 2.2 模块入口新增「商店」按钮

| 父物体 | 子物体名 | 组件 | 说明 |
|--------|----------|------|------|
| `界面/详情/主页/模块入口` | `商店` | Button | 与「我」「任务」「成就」并列的入口按钮 |

**操作步骤：**

1. 在 Hierarchy 中找到 `界面` → `详情` → `主页` → `模块入口`
2. 复制「任务」或「成就」按钮，重命名为 **商店**
3. 修改按钮文案为「商店」
4. 确保挂有 **Button** 和 **Comp**（若项目要求）

---

## 三、模板区

### 3.1 商品列表项模板：`商品模板`

| 父物体 | 子物体名 | 组件 | 说明 |
|--------|----------|------|------|
| `界面/模板` | `商品模板` | Comp | 单个商品行的预制根 |
| `商品模板` | `商品名` | TextMeshProUGUI | 商品名称 |
| `商品模板` | `价格` | TextMeshProUGUI | 显示「价格:xxx」 |
| `商品模板` | `库存` | TextMeshProUGUI | 显示「库存:x」或「无限」 |
| `商品模板` | `类型` | TextMeshProUGUI | 显示「类型:物品」等 |
| `商品模板` | `购买` | Button | 购买按钮 |

**操作步骤：**

1. 在 `界面/模板` 下新建空物体，命名为 **商品模板**
2. 参考「任务模板」或「成就模板」的布局（横向排列：名称、价格、库存、类型、按钮）
3. 添加子物体并**严格使用上表中的名称**：
   - `商品名`：TextMeshProUGUI
   - `价格`：TextMeshProUGUI
   - `库存`：TextMeshProUGUI
   - `类型`：TextMeshProUGUI
   - `购买`：Button
4. 可将 `商品模板` 做成 Prefab，拖回 `模板` 下（或保持为场景内物体，脚本会 Instantiate）

---

### 3.2 创建商品表单模板：`添加商品模板`

| 父物体 | 子物体名 | 组件 | 说明 |
|--------|----------|------|------|
| `界面/模板` | `添加商品模板` | Comp | 创建商品表单根 |
| `添加商品模板` | `商品名` | TMP_InputField 或 TextMeshProUGUI | 输入商品名 |
| `添加商品模板` | `价格` | TMP_InputField 或 TextMeshProUGUI | 输入价格（整数） |
| `添加商品模板` | `库存` | TMP_InputField 或 TextMeshProUGUI | 输入库存（1=一次性，9999=无限） |
| `添加商品模板` | `类型` | TMP_InputField 或 TextMeshProUGUI | 输入类型，默认「物品」 |
| `添加商品模板` | `创建完成` | Button | 提交创建 |

**操作步骤：**

1. 在 `界面/模板` 下新建空物体，命名为 **添加商品模板**
2. 参考「添加任务模板」或「添加成就模板」的布局
3. 添加子物体并**严格使用上表中的名称**：
   - `商品名`：TMP_InputField（可编辑）或 TextMeshProUGUI（只读占位）
   - `价格`：TMP_InputField
   - `库存`：TMP_InputField
   - `类型`：TMP_InputField（可选，默认为「物品」）
   - `创建完成`：Button
4. 默认可将 `添加商品模板` 设为 **SetActive(false)**，脚本会 Instantiate 后设为 true

---

## 四、状态栏与流程

- **创建 / 取消创建**：与任务/成就共用状态栏的「创建」「取消创建」按钮
- 进入商店后，点击「创建」会显示「添加商品模板」表单
- 点击「取消创建」会返回商品列表并刷新

**无需额外配置**，只要任务/成就的状态栏已存在即可。

---

## 五、节点名速查表（必须完全一致）

| 路径 | 节点名 |
|------|--------|
| `详情` 的子物体 | `商店` |
| `商店` 的子物体 | `父物体` |
| `模块入口` 的子物体 | `商店`（Button） |
| `模板` 的子物体 | `商品模板` |
| `模板` 的子物体 | `添加商品模板` |
| `商品模板` 的子物体 | `商品名`、`价格`、`库存`、`类型`、`购买` |
| `添加商品模板` 的子物体 | `商品名`、`价格`、`库存`、`类型`、`创建完成` |

---

## 六、自检清单

运行游戏后，请逐项确认：

- [ ] 主页「模块入口」出现「商店」按钮
- [ ] 点击「商店」进入商店页，标题显示「商店」
- [ ] 商店页显示「创建」按钮，点击后进入创建商品表单
- [ ] 填写商品名、价格、库存、类型，点击「创建完成」后商品出现在列表
- [ ] 余额充足时「购买」可点击，点击后 `Debug.Log` 输出「购买成功」
- [ ] 余额不足或库存为 0 时「购买」为灰色不可点击
- [ ] 返回主页后，交互点显示正确（未因购买而错误扣除）

---

## 七、若 Cond.Get 报错

若控制台出现类似「找不到 商店」「找不到 商品模板」等错误：

1. 检查 Hierarchy 中节点名是否与上表**完全一致**（区分中英文、空格）
2. 确认 `Cond.Get` 的 parent 正确（如 `商店` 必须是 `详情` 的**直接子物体**）
3. 确认对应物体挂有脚本所需组件（Button、Comp、TextMeshProUGUI 等）

---

## 八、Prefab 建议（可选）

- 可将 `商品模板`、`添加商品模板` 做成 Prefab 放入 `Resources` 或项目目录，再在场景的 `模板` 下引用，便于复用与版本管理
- 保持与「任务模板」「成就模板」相同的项目规范即可
